import react from 'react';
import ReactDOM from 'react-dom';

function Header(){
    return(
        <header>
        <div>
            <h1>keep notes </h1>
        </div>
        </header>
    )
}
export default Header;